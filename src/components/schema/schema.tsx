import styles from './schema.module.scss';

export default function Schema() {
  return (
    <div>
      <button className={styles.btn}><p className={styles.name}>Shema</p></button>
    </div>
  );
}
