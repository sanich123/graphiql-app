export const enum SOCIAL_NETWORKS {
  github = 'Github',
  google = 'Google',
}

export const enum AUTH_PLACES {
  login = 'Login',
  register = 'Register',
  reset = 'Reset',
}

export const enum AUTH_PLACES_RU {
  login = 'Логин',
  register = 'Регистрация',
  reset = 'Сброс',
}

export const enum LANGUAGES {
  ru = 'ru',
  en = 'en',
}

export const enum THEMES {
  dark = 'dark',
  light = 'light',
}

export const enum ROUTES {
  main = '/',
  login = '/login',
  graphiql = '/graphiql',
}

export const enum COLORS {
  almostBlack = '#161D25',
  white = 'white',
}

export const enum TEXTAREA_PLACES {
  request = 'request',
  variables = 'variables',
}
